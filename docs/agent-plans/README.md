# Agent Plans

Durable plans for Hoisa issues live here.

Use filenames of the form:

```text
<issue-number>-<short-slug>.md
```

Plans should describe intended behavior, affected files, interfaces, tests,
risk gates, simplification evidence, assumptions, out-of-scope items, and
revision history. Mutable approval state belongs in tracker metadata, not plan
front matter.
